{
  "name": "dbd-utils",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "bun run --filter @dbd-utils/web dev",
    "dev:api": "bun run --filter @dbd-utils/api dev",
    "build": "bun run --filter @dbd-utils/web build",
    "deploy:api": "bun run --filter @dbd-utils/api deploy"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
