{
  "name": "dbd-utils",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "deploy:api": "bun run --filter @dbd-utils/api deploy"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}